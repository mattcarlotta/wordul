import type { Metadata } from "next";
import type { ReactNode } from "react";
import localFont from "next/font/local";
import clsx from "clsx";
import "./globals.css";

const wordul = localFont({
    src: [
        {
            path: "./fonts/wordul-500.woff2",
            weight: "500",
            style: "normal"
        },
        {
            path: "./fonts/wordul-600.woff2",
            weight: "600",
            style: "normal"
        },
        {
            path: "./fonts/wordul-700.woff2",
            weight: "700",
            style: "normal"
        }
    ],
    display: "swap"
});

export const metadata: Metadata = {
    title: "Wordul - An Unexpected Journey",
    description: "Generated by create next app"
};

export default function RootLayout({
    children
}: Readonly<{
    children: ReactNode;
}>) {
    return (
        <html lang="en">
            <body
                className={clsx(wordul.className, "antialiased min-h-screen")}
                cz-shortcut-listen="true"
            >
                {children}
            </body>
        </html>
    );
}
